bash - contains the scripts that download, compile, and locally install dependencies
build - contains the build of the dependencies
install - where the dependencies get installed
md5 - md5sum sums of each dependency to ensure they were downloaded correctly
README - the file you're reading now, duh
source - the source code archives of each dependency (which can be fetched by the bash scripts referenced above)
